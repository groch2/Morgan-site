doctype html

html(lang="en")

    head
        meta(charset="utf-8")
        link(rel="stylesheet" href="./styles.css")

    body
        each p in paintingFiles
            div(class="picture-container")
                img(src=p)
